#![allow(unused)]

mod  in_function;
mod in_struct;
mod in_enum;

fn main() 
{
  // In Function Definitions
  in_function::run();

  // In Struct Definitions
  in_struct::run();
  
  // in Enum
  in_enum::run();
}
